set daemon 10
set mailserver localhost
set alert phuongnd08@gmail.com
set httpd port 2812 address localhost
  allow localhost
<% if ENV["MONIT_LOGIN"] && ENV["MONIT_PASSWORD"] %>
  allow
  allow <%= ENV["MONIT_LOGIN"] %>:<%= ENV["MONIT_PASSWORD"]%>
<% end %>
include /etc/monit.d/*
